import { GameDocument } from "../game";
import { InputId } from "../../../src/models/inputs";
import { InputCtxFactory } from "./input";

function getContextFunction(input: InputId): InputCtxFactory<any> {
    if (input === InputId.SelectCharacter) {
        return getAv
    }
}

export function makeRequest(gameDoc: GameDocument, inputId: InputId, userId: string) {
    const ctx =
    if () {

    }
}